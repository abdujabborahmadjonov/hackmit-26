[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -q --strict-markers
markers =
    integration: requires a PostgreSQL database with pgvector
filterwarnings =
    ignore::DeprecationWarning
