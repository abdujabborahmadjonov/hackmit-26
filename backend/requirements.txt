# Runtime dependencies only - what the deployed container needs.
# Test and demo-data tooling lives in requirements-dev.txt.

# --- web ---
fastapi==0.141.1
uvicorn[standard]==0.34.0
python-multipart==0.0.32
# Transitive (via fastapi), pinned here so the security floor is explicit:
# everything below 1.3.1 carries known advisories.
starlette==1.6.0

# --- data ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6
greenlet==3.1.1

# --- validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- auth ---
pyjwt==2.14.0
argon2-cffi==23.1.0

# --- search engine (optional backend, SEARCH_PROVIDER=elasticsearch) ---
elasticsearch[async]==8.17.0

# --- embeddings / storage over HTTP ---
httpx==0.28.1

# Optional: S3-compatible storage (STORAGE_PROVIDER=s3).
# Supabase Storage needs nothing extra - it uses httpx.
# boto3==1.35.90
